<%= link_to 'Make A Screenshot Request', new_screenshotreq_path %>
<h2>Stored Requests</h2>
<% @screenshotreqs.each do |request| %>
  <div>
    <p><%= request.name %></p>
    <p><%= link_to 'Show', screenshotreq_path(request) %> 
  </div>
<% end %>